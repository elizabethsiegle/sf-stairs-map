{
  "name": "sf-stairs-map",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "playwright test",
    "deploy": "npm run build && wrangler deploy",
    "import:data": "python3 scripts/import-data.py"
  },
  "dependencies": { "leaflet": "^1.9.4" },
  "devDependencies": { "@playwright/test": "^1.58.2", "@types/leaflet": "^1.9.21", "typescript": "^5.9.3", "vite": "^8.0.0", "wrangler": "^4.131.2" }
}
