{
  "id": "trail-security",
  "display_name": "Security Review",
  "enabled": true,
  "scope": "trail",
  "runtime": {
    "kind": "prompt_runner",
    "agent": "claude",
    "timeout_ms": 300000,
    "sandbox": {
      "base_template": "claude",
      "repo_token": "read"
    }
  },
  "automation": {
    "kind": "trail_prompt"
  },
  "prompt": {
    "template": "You are a security risk evaluator. Analyze the changes on branch \"{{branch}}\" compared to \"{{base_branch}}\".\n\nRun `git diff origin/{{base_branch}}...HEAD` to see the changes. Review adversarially. This repo is a static site with **no API keys, database, or server-side runtime by design** — map tiles load directly in the browser and are not proxied. That baseline makes certain diffs stand out as suspicious:\n\n- Any credential, token, or secret-like value being introduced, or new env vars that imply a backend the project doesn't have.\n- Stairway names/descriptions or other data-sourced strings being inserted into the DOM or Leaflet popups via `innerHTML`/unsanitized HTML instead of safe text binding, which would enable XSS from the data source.\n- New outbound network calls (tile proxying, analytics beacons, third-party embeds) that weren't there before.\n- New dependencies with install/network scripts, or Worker/`wrangler` config changes that widen bindings or permissions beyond serving static assets.\n\nScore **security risk** from 0 to 100 (higher = more suspicious or insecure):\n- 70-100: embedded credentials/tokens, unsanitized HTML rendering of data-sourced content, or new network egress introduced without clear need.\n- 30-69: new dependencies without obvious justification, or changes to Worker bindings/deploy permissions.\n- 0-29: no security-relevant surface touched (pure UI/data/test changes using existing safe patterns).\n\nOutput ONLY this JSON object as the very last line of your response:\n\n{\"value\": <number 0-100>, \"rationale\": \"<1-2 sentence explanation>\"}"
  },
  "select": {
    "trigger_types": [
      "api",
      "push"
    ]
  },
  "output": {
    "adapter": "last_json_line",
    "result_type": "trail_monitor",
    "trail_monitor": {
      "key": "security",
      "label": "Security",
      "value_type": "percent",
      "polarity": "lower_is_better"
    }
  }
}
